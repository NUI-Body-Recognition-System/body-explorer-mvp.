- 2026-07-15: **PRODUCTION FIX** — Fixed viteStaticCopy to flatten MediaPipe WASM runtime and model files to `dist/wasm/mediapipe/` (was nesting under `node_modules/...`). Added `patchMediapipeLoader()` to fix upstream `custom_dbg` and `ModuleFactory` bugs in `@mediapipe/tasks-vision@0.10.35`. Verified with real camera in production preview build.
- 2026-07-15: **INTENTIONAL TRADE-OFF** — Deleted six vendored `src/wasm/mediapipe/vision_wasm*` runtime files (3 JS + 3 WASM, ~44 MB). Production build now copies the runtime directly from `node_modules/@mediapipe/tasks-vision/wasm/` during `npm run build`. The two `.task` model files (`pose_landmarker_lite.task`, `face_landmarker.task`) are retained since they are not shipped by the npm package. There is NO offline-vendored fallback for the WASM runtime after this deletion — a fresh `npm install` is required before building.
- 2026-07-15: Behavior-neutral code cleanup: 194 dead-code lines removed, zero test regressions (91/91 unit tests), 28-module production build confirmed.
- 2026-07-15: Corrected Fallback 2 tier safety to prevent harder-target leakage and added protected cross-level boundary-repeat handling with regression coverage.
- 2026-07-15: Added 23 localized body-function facts with 115 Piper narrations, narration-aware feedback timing, and five-language regression coverage.
